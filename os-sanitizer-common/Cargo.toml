[package]
name = "os-sanitizer-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { path = "../../aya/aya", optional = true }
aya-bpf-cty = { path = "../../aya/bpf/aya-bpf-cty" }
enum-iterator = "1.4.1"

[lib]
path = "src/lib.rs"
